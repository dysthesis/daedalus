# Daedalus

A (WIP) tmux flake.

In copy mode, press Enter to copy selected text, then paste it into an application's file picker or command field. The `o`/`O` execution shortcuts were removed because selections must not execute as shell source.
